#!/usr/bin/env groovy
podTemplate {
	node(POD_LABEL) {
		stage('Clone repository') {
			checkout scm
		}
	}
}

podTemplate {
	node(POD_LABEL) {
		stage('Build Image ') {
			echo 'built the image successfully!'
		}
	}
}

